import { useRef } from "react";
 
const RefTutorial = () =>{

    return(
        <div>
            <h1>Rosen</h1>
            <input type="text" placeholder="enter a name" />
            <button>Change Name</button>
        </div>
    )
}